const {find} = require('./find')
const {query} = require('./query')
const {queryByUpdated} = require('./query-by-updated')
const {save} = require('./save')
const {remove} = require('./remove')

module.exports = {find, query, queryByUpdated, save, remove}
