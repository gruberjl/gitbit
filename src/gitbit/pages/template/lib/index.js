const {getTemplate} = require('./get-template')
const {save} = require('./save')
const {remove} = require('./remove')
const {inlineCss} = require('./inline-css')

module.exports = {getTemplate, save, remove, inlineCss}
