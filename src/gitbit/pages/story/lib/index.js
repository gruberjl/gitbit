const {getStory} = require('./get-story')
const {getImages} = require('./get-images')
const {publish} = require('./publish')
const {remove} = require('./remove')

module.exports = {
  getStory, getImages, publish, remove
}
