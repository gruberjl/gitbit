const {FeaturedImages} = require('./featured-images')
const {Slug} = require('./slug')
const {PublishTime} = require('./publish-time')
const {Templates} = require('./templates')

module.exports = {FeaturedImages, Slug, PublishTime, Templates}
